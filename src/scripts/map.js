const numbers = [1, 2, 3, 4];

const doubledNumbers = numbers.map( num => num * 2);

console.log("--- numbers ---") // [XXX] REMOVE BEFORE COMMITING
console.log(numbers) // [XXX] REMOVE BEFORE COMMITING

console.log("--- doubledNumbers ---") // [XXX] REMOVE BEFORE COMMITING
console.log(doubledNumbers) // [XXX] REMOVE BEFORE COMMITING
